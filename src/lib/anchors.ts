// Anchors that pages link to across route boundaries. Kept out of the route
// files themselves so linking to a section never pulls in that route's chunk.

/** The concept graph on a role's path page. */
export const GRAPH_ANCHOR = 'path-graph'
